# Login Assignment

This project includes a React client and a Node.js (Express) server.  
The `.env` file is already included inside the `server` folder as required.

---

## Running the Backend (Server)

Navigate to the server folder:

cd server
npm install
node index.js

## Running the Frontend (Client)

Navigate to the client folder:

cd client
npm install
npm start




